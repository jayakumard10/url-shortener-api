name: ci

# No repository secrets required. The agentic-events dependency lives in
# agentic-sdlc-eventbus, which is public, so it resolves over anonymous HTTPS -
# a fork of this repo gets a green build with nothing to configure.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests with coverage
        # Measured 92% (35 tests). The floor sits just under it so coverage can
        # only ratchet up. The two gaps below it are deliberate and documented in
        # the README: db.py's real session bodies, and telemetry.py's producer
        # construction, which needs a reachable-or-unreachable broker and is
        # covered by the compose integration run instead.
        run: pytest --cov=url_shortener --cov-report=term-missing --cov-fail-under=91

  compose-smoke-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Write local secret file for compose
        run: |
          mkdir -p secrets
          printf 'ci-postgres-password' > secrets/postgres_password.txt

      - name: Build and start
        run: docker compose up -d --build

      - name: Wait for API healthy
        run: |
          for i in $(seq 1 30); do
            status="$(docker inspect --format='{{.State.Health.Status}}' url-shortener-api || echo starting)"
            echo "attempt $i: $status"
            if [ "$status" = "healthy" ]; then
              exit 0
            fi
            sleep 5
          done
          echo "api never became healthy" >&2
          docker compose logs
          exit 1

      - name: Smoke test /health
        run: curl -sf http://localhost:8000/health

      - name: Tear down
        if: always()
        run: docker compose down -v
